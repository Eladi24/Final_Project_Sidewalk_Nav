# Stage B (CPU-only) — install this to run the geometry pipeline locally.
# No torch or GPU required.
numpy>=1.24
opencv-python>=4.8
open3d>=0.18
scikit-learn>=1.3
pyyaml>=6.0
pyttsx3>=2.90        # optional audio alerts; safe to skip if TTS is unneeded
